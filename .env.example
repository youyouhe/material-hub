# MaterialHub Configuration
# Database (SQLite, relative to backend/)
DB_PATH=data/materials.db

# Server
HOST=0.0.0.0
PORT=8101

# OCR Service
# Docker容器中访问宿主机服务地址
# Linux上使用 host.docker.internal:8010 (需要docker-compose中配置extra_hosts)
# 或者直接使用宿主机IP地址，如 http://192.168.1.100:8010
OCR_SERVICE_URL=http://host.docker.internal:8010

# OCR请求超时时间（秒），根据OCR服务处理速度调整
# 复杂图片或大图片可能需要更长时间
OCR_TIMEOUT=120

# LLM Provider for Intelligent OCR Extraction
# 支持: deepseek | openrouter | anthropic
LLM_PROVIDER=deepseek

# DeepSeek Configuration
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# OpenRouter Configuration (如果使用OpenRouter)
# OPENROUTER_API_KEY=sk-or-your-openrouter-api-key
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# OPENROUTER_SITE_URL=https://materialhub.local
# OPENROUTER_APP_NAME=MaterialHub

# Anthropic Configuration (如果使用Anthropic直连)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Smart OCR Filtering
# 是否启用LLM智能预筛选材料（推荐：节省OCR成本和时间）
# true: 使用LLM判断哪些材料需要OCR
# false: 使用简单关键词过滤
ENABLE_LLM_FILTER=true
